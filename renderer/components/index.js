// renderer/components/index.js
// Assumes tier1, tier2, and tier3 are loaded in the HTML before this index or are bundled.
// For Vanilla JS without a bundler, we just ensure global namespaces are correct.
console.log('PF Components Loaded');
